import { IconButton, styled } from "@mui/material";

export const EditImgButton = styled(IconButton)({
  backgroundColor: "rgba(0, 0, 0, 0.5)",
  "&:hover": {
    backgroundColor: "rgba(0, 0, 0, 0.7)",
  },
});
